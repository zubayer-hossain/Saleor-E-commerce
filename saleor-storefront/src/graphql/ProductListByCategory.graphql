query ProductListByCategory(
	$slug: String!
	$channel: String!
	$languageCode: LanguageCodeEnum!
	$first: Int
	$after: String
	$sortBy: ProductOrder
	$filter: ProductFilterInput
) {
	category(slug: $slug) {
		id
		name
		slug
		description
		seoDescription
		seoTitle
		backgroundImage {
			url
			alt
		}
		translation(languageCode: $languageCode) {
			name
			description
			seoTitle
			seoDescription
		}
		products(first: $first, after: $after, channel: $channel, sortBy: $sortBy, filter: $filter) {
			edges {
				node {
					...ProductListItem
				}
				cursor
			}
			pageInfo {
				hasNextPage
				hasPreviousPage
				startCursor
				endCursor
			}
			totalCount
		}
	}
}
